/**
 * Type definitions for the HomePage5 component and its props
 */

type HomePage5Props = {
  // Add any props here if the component accepts any
  // Currently, the component doesn't accept any props
}

/**
 * The HomePage5 component serves as the main landing page of the application.
 * It composes various section components to create the complete home page layout.
 */
declare const HomePage5: React.FC<HomePage5Props>;

export default HomePage5;
