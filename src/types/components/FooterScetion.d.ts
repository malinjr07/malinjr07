declare module 'FooterScetion' {
  // No specific props for this component
  
  // Define the service type used in the component
  interface Service {
    slug: string;
    title: string;
    // Add other properties from the Services API if needed
  }
}
